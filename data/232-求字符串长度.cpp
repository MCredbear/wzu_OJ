#include <bits/stdc++.h>
using namespace std;

int main()
{
    for (;;)
    {
        string s;
        getline(cin, s);
        if (s != "")
            cout << s.length() << endl;
        else
            break;
    }
}
